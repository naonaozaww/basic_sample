<!-- createアクションはparams[:id]が無いので意図的にparams[:board_id]を送るようにする -->
<%= link_to bookmarks_path(board_id: board.id), id: "js-bookmark-button-for-board-#{board.id}", class:"float-right", method: :post do %>
  <%= icon 'far', 'star' %>
<% end %>
